# Copy to .env (never committed) and fill in.
# These are baked into the client bundle at build time — see SPEC.md §9.8
# for why the sync secret is accepted to be public.
VITE_SYNC_URL=https://cacotas-sync.soker.workers.dev/sync
VITE_SYNC_SECRET=
